## EPS (sources)

Dépose ici les **EPS originaux** de tes vraies étiquettes.

Le navigateur ne peut pas les rendre directement: il faut exporter des versions web (PNG/SVG) dans:
- `assets/backgrounds/`

